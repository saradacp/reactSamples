ReactStarter
---------------

Checkout this repository, install dependencies, then start the gulp process with the following:

```
	> git clone https://github.com/saradacp/ReactStarter.git
	> cd ReactStarter
	> npm install
	> gulp
```
